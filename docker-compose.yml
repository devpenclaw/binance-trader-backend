services:
  trader-backend:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./config.json:/app/config.json
      - ./state.json:/app/state.json
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3001
